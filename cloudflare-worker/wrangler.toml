name = "portfolio-contact-email"
main = "worker.js"
compatibility_date = "2024-01-01"

# Uncomment and configure if you want to use a custom route
# routes = [
#   { pattern = "email.rohanthapashrestha.com.np", zone_name = "rohanthapashrestha.com.np" }
# ]

# Workers will be deployed on the free tier by default
# Free tier: 100,000 requests per day
